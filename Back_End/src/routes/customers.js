const express = require('express');
const router = express.Router();


router.get("/", );
router.get("/:maKhachHang", );
router.post("/register", );
router.put("/:maKhachHang", );
router.delete("/:maKhachHang", );
router.post("/login",);

module.exports = router;